# ===== Web framework =====
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0

# ===== LangChain (modern stack: langchain.agents.create_agent) =====
langchain>=1.0.0
langchain-core>=0.3.0
langgraph>=0.2.0
# Provides the "openrouter:" provider used by init_chat_model / create_agent
langchain-openrouter>=0.1.0
# MCP tools (Phase 2): connect MCP servers and expose their tools to the agent
langchain-mcp-adapters>=0.2.0

# ===== Built-in tools =====
ddgs>=6.0.0          # DuckDuckGo search backend for the web_search tool

# ===== Tests =====
pytest>=8.0.0
pytest-asyncio>=0.23.0
